import 'package:flutter/material.dart';

class RutasInfS extends StatelessWidget {
  const RutasInfS({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text(
        'RutasInfS',
        style: TextStyle(fontSize: 32, fontWeight: FontWeight.bold),
      ),
    );
  }
}
