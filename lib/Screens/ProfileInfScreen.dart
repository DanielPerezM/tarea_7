import 'package:flutter/material.dart';

class ProfileInfS extends StatelessWidget {
  const ProfileInfS({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text(
        'ProfileInfS',
        style: TextStyle(fontSize: 32, fontWeight: FontWeight.bold),
      ),
    );
  }
}
